pipeline {
    agent any
    stages {
        stage('package') {
            steps {
            node('build') {
               git 'https://github.com/asquarezone/game-of-life.git'
               sh label: '', script: '''mvn clean package'''
            }
	}
}
}
}
